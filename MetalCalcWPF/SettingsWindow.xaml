<Window x:Class="MetalCalcWPF.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Настройки цеха" Height="650" Width="550" WindowStartupLocation="CenterScreen">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TabControl Grid.Row="0">

            <TabItem Header="💰 Экономика">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel Margin="10">
                        <GroupBox Header="Лазерный участок" Margin="0,0,0,10" Padding="5">
                            <StackPanel>
                                <TextBlock Text="Оклад оператора (тг):"/>
                                <TextBox Margin="0,2,0,5" Text="{Binding SalaryText, UpdateSourceTrigger=PropertyChanged}"/>
                                <TextBlock Text="График (Дней / Часов):"/>
                                <StackPanel Orientation="Horizontal">
                                    <TextBox Width="50" Margin="0,0,5,0" Text="{Binding DaysText, UpdateSourceTrigger=PropertyChanged}"/>
                                    <TextBox Width="50" Text="{Binding HoursText, UpdateSourceTrigger=PropertyChanged}"/>
                                </StackPanel>
                            </StackPanel>
                        </GroupBox>

                        <GroupBox Header="Гибочный участок" Margin="0,0,0,10" Padding="5">
                            <StackPanel>
                                <TextBlock Text="Оклад оператора (тг):"/>
                                <TextBox Margin="0,2,0,5" Text="{Binding BendingSalaryText, UpdateSourceTrigger=PropertyChanged}"/>
                            </StackPanel>
                        </GroupBox>

                        <GroupBox Header="Расходы цеха" Padding="5">
                            <StackPanel>
                                <TextBlock Text="Электричество (тг/кВт):"/>
                                <TextBox Margin="0,2,0,5" Text="{Binding ElectricityText, UpdateSourceTrigger=PropertyChanged}"/>
                                <TextBlock Text="Амортизация (тг/час):"/>
                                <TextBox Margin="0,2,0,5" Text="{Binding AmortizationText, UpdateSourceTrigger=PropertyChanged}"/>
                            </StackPanel>
                        </GroupBox>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>

            <TabItem Header="🏗️ Материалы">
                <StackPanel Margin="10">
                    <GroupBox Header="Торговая наценка" Padding="10">
                        <StackPanel>
                            <TextBlock Text="Наценка на металл (%):"/>
                            <TextBox Margin="0,5,0,5" FontWeight="Bold" Text="{Binding MaterialMarkupText, UpdateSourceTrigger=PropertyChanged}"/>
                            <TextBlock Text="Пример: Если закуп 355 тг, а наценка 30%, продаем по 461 тг."
                                       FontSize="10" Foreground="Gray" TextWrapping="Wrap"/>
                        </StackPanel>
                    </GroupBox>

                    <TextBlock Text="* Редактирование базовых цен металлов доступно в разделе 'База данных'."
                               Foreground="Gray" Margin="0,20,0,0" TextAlignment="Center" TextWrapping="Wrap"/>
                </StackPanel>
            </TabItem>

            <!-- ✅ НОВАЯ ВКЛАДКА: КИСЛОРОД -->
            <TabItem Header="💨 Кислород">
                <StackPanel Margin="10">
                    <GroupBox Header="Параметры баллона" Padding="10">
                        <StackPanel>
                            <TextBlock Text="Объем баллона (литров):"/>
                            <TextBox Margin="0,5,0,10" Text="{Binding OxygenVolumeText, UpdateSourceTrigger=PropertyChanged}"/>
                            
                            <TextBlock Text="Давление в баллоне (атмосфер):"/>
                            <TextBox Margin="0,5,0,10" Text="{Binding OxygenPressureText, UpdateSourceTrigger=PropertyChanged}"/>
                            
                            <TextBlock Text="Расход при резке (л/мин):"/>
                            <TextBox Margin="0,5,0,10" Text="{Binding OxygenFlowText, UpdateSourceTrigger=PropertyChanged}"/>
                            
                            <TextBlock Text="Стоимость заправки (тг):"/>
                            <TextBox Margin="0,5,0,10" FontWeight="Bold" Text="{Binding OxygenPriceText, UpdateSourceTrigger=PropertyChanged}"/>
                        </StackPanel>
                    </GroupBox>

                    <Border Background="#E3F2FD" Padding="10" CornerRadius="5" Margin="0,10,0,0">
                        <StackPanel>
                            <TextBlock Text="📊 Расчетные показатели:" FontWeight="Bold" Margin="0,0,0,5"/>
                            <TextBlock Text="{Binding OxygenCalculationInfo}" Foreground="#1976D2" TextWrapping="Wrap"/>
                        </StackPanel>
                    </Border>

                    <TextBlock Text="💡 Кислород используется при резке металла толщиной 12+ мм."
                               FontSize="10" Foreground="Gray" Margin="0,10,0,0" TextWrapping="Wrap"/>
                </StackPanel>
            </TabItem>

            <TabItem Header="⚙️ Технология">
                <StackPanel Margin="10">
                    <GroupBox Header="Коэффициенты сложности" Padding="5">
                        <StackPanel>
                            <TextBlock Text="Порог 'Тяжелого' металла (мм):"/>
                            <TextBox Margin="0,2,0,5" Text="{Binding ThresholdText, UpdateSourceTrigger=PropertyChanged}"/>
                            <TextBlock Text="Доплата за тяжесть (тг/дет):"/>
                            <TextBox Margin="0,2,0,5" Text="{Binding HeavyCostText, UpdateSourceTrigger=PropertyChanged}"/>
                        </StackPanel>
                    </GroupBox>

                    <GroupBox Header="Сварка" Padding="5" Margin="0,10,0,0">
                        <StackPanel>
                            <TextBlock Text="Стоимость сварки (тг/см):"/>
                            <TextBox Margin="0,2,0,5" Text="{Binding WeldCostText, UpdateSourceTrigger=PropertyChanged}"/>
                        </StackPanel>
                    </GroupBox>
                </StackPanel>
            </TabItem>

        </TabControl>

        <Button Grid.Row="1" Content="💾 Сохранить настройки" Height="40" Command="{Binding SaveCommand}"
                Background="#4CAF50" Foreground="White" FontWeight="Bold" Margin="0,10,0,0"/>
    </Grid>
</Window>
